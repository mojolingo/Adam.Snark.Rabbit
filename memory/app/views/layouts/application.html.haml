!!! 5
%html(lang="en")
  %head
    %meta(charset="utf-8")
    %meta(name="viewport" content="width=device-width, initial-scale=1.0")
    %title= content_for?(:title) ? yield(:title) + " | Adam 'Snark' Rabbit" : "Adam 'Snark' Rabbit"
    %link(href='http://fonts.googleapis.com/css?family=Open+Sans:400,300' rel='stylesheet' type='text/css')
    = csrf_meta_tags
    / Le HTML5 shim, for IE6-8 support of HTML elements
    /[if lt IE 9]
      = javascript_include_tag "http://html5shim.googlecode.com/svn/trunk/html5.js"
    = stylesheet_link_tag "application", :media => "all"
    %link(href="images/favicon.ico" rel="shortcut icon")
    %link(href="images/apple-touch-icon.png" rel="apple-touch-icon")
    %link(href="images/apple-touch-icon-72x72.png" rel="apple-touch-icon" sizes="72x72")
    %link(href="images/apple-touch-icon-114x114.png" rel="apple-touch-icon" sizes="114x114")

  %body
    .navbar.navbar-fixed-top
      .navbar-inner
        .container
          %a.btn.btn-navbar(data-target=".nav-collapse" data-toggle="collapse")
            %span.icon-bar
            %span.icon-bar
            %span.icon-bar
          %a.brand(href="/") ASR
          - if current_user
            %a.brand(href="#")= "Logged in as #{current_user.name} (#{user_auth_source_string(current_user)})"
          .container.nav-collapse
            %ul.nav
              - if current_user
                %li#profile= link_to "My Profile", my_profile_path
                %li#logout= link_to "Logout", destroy_user_session_path
              - else
                %li#login_github= link_to "Login with Github", omniauth_authorize_path(:user, :github)
                %li#login_twitter= link_to "Login with Twitter", omniauth_authorize_path(:user, :twitter)

    .container
      = render :partial => "shared/flash_messages", :locals => {:flash => flash}

      .content
        .row
          .span12
            = yield

      %footer
        %span &copy; Mojo Lingo LLC 2013 - 
        %span{class: 'curtain-caller'} Curtains!
    /
      Javascripts
      \==================================================
    / Placed at the end of the document so the pages load faster
    = javascript_include_tag "application"
